[package]
name = "site"
version = "0.1.0"
edition = "2021"

# Defines a size-optimized profile for the WASM bundle in release mode
[profile.wasm-release]
strip = "debuginfo"
inherits = "release"
opt-level = 's'
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
rocket = "0.5.1"
